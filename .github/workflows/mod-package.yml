name: Build Mod Package

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

env:
  TARGET_PLATFORM: windows
  TARGET_ARCH: x64
  # Reserved, disabled targets: windows_arm, macos_x64, macos_arm, linux_x64, linux_arm.
  MOD_NAME: ArchiveXL

jobs:
  package:
    runs-on: windows-2025
    outputs:
      artifact: ${{ steps.meta.outputs.artifact }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: xmake-io/github-action-setup-xmake@v1
      - name: Apply tag version to source
        if: github.ref_type == 'tag'
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.TrimStart('v')
          (Get-Content xmake.lua -Raw) -replace 'set_version\("[^"]+"', "set_version(`"$version`"" | Set-Content xmake.lua
      - name: Install WolvenKit CLI
        run: dotnet tool install --global wolvenkit.cli
      - name: Build bundled archive from source
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path bundle/source/archive | Out-Null
          cp77tools import -p bundle/source/raw -o bundle/source/archive --keep
          $output = 'bundle/packed/archive/pc/mod'
          New-Item -ItemType Directory -Force -Path $output | Out-Null
          cp77tools pack -p bundle/source/archive -o $output
          $archive = Get-ChildItem $output -Filter *.archive | Select-Object -First 1
          if (-not $archive) { throw "WolvenKit did not produce an archive" }
          Move-Item -LiteralPath $archive.FullName -Destination "$output/ArchiveXL.archive" -Force
      - name: Build windows_x64
        run: |
          xmake config --arch=x64 --mode=release --yes
          xmake build
      - name: Package
        shell: pwsh
        run: |
          $dll = Get-ChildItem build -Recurse -Filter ArchiveXL.dll | Select-Object -First 1 -ExpandProperty FullName
          if (-not $dll) { throw "ArchiveXL.dll was not produced" }
          ./tools/dist/package-mod.ps1 -ReleaseBin $dll
      - name: Normalize artifact name
        id: meta
        shell: pwsh
        run: |
          $version = if ($env:GITHUB_REF_TYPE -eq 'tag') { $env:GITHUB_REF_NAME.TrimStart('v') } else { $stable = @(git tag -l --sort=-v:refname | Where-Object { $_ -match '^v?\d+\.\d+\.\d+$' }); $any = @(git tag -l --sort=-v:refname | Where-Object { $_ -match '^v?\d' }); $tag = $(@($stable + $any) | Select-Object -First 1); if ($tag) { $tag.TrimStart('v') } else { '0.0.0' } }
          $suffix = if ($env:GITHUB_REF_TYPE -eq 'tag') { '' } else { "_$(Get-Date -Format 'yyyy.M').$((git rev-list --count --since="$(Get-Date -Day 1 -Format yyyy-MM-dd)" HEAD))" }
          $artifact = "${env:MOD_NAME}_v${version}_${env:TARGET_PLATFORM}_${env:TARGET_ARCH}${suffix}.zip"
          $packageVersion = & ./tools/dist/steps/get-version.ps1
          $source = Get-Item -LiteralPath "build/dist/${env:MOD_NAME}-${packageVersion}.zip" -ErrorAction Stop
          Move-Item -LiteralPath $source.FullName -Destination "build/dist/$artifact" -Force
          "artifact=$artifact" >> $env:GITHUB_OUTPUT
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.artifact }}
          path: build/dist/${{ steps.meta.outputs.artifact }}
          if-no-files-found: error
      - name: Create draft release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$env:GITHUB_REF_NAME" "build/dist/${{ steps.meta.outputs.artifact }}" --draft --generate-notes --verify-tag
